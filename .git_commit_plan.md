# Git Commit Plan

This document outlines the planned commits for review-friendly Pull Requests.

---

## Commit 1: CORS Configuration (Critical Security Fix)

**Type:** `security`  
**Priority:** Critical

**Files:**
- `client/core/rs/src/entities/config/core.rs` - Add CORS fields to CoreConfig
- `bin/core/src/config.rs` - Parse CORS environment variables
- `bin/core/src/main.rs` - Dynamic CORS layer creation
- `config/core.config.toml` - CORS documentation

**Message:**
```
security: Add configurable CORS support

- Add cors_allowed_origins and cors_allow_credentials fields to CoreConfig
- Support environment variables KOMODO_CORS_ALLOWED_ORIGINS and KOMODO_CORS_ALLOW_CREDENTIALS
- Implement dynamic CORS layer creation based on configuration
- Backward compatible: empty list allows all origins
- Add comprehensive documentation in config file

This addresses the critical security issue where CORS allowed all origins.
Production deployments should configure specific allowed origins.

Related: Security analysis identified CORS as critical issue
```

---

## Commit 2: Security Headers

**Type:** `security`  
**Priority:** High

**Files:**
- `bin/core/src/main.rs` - Security headers function

**Message:**
```
security: Add security headers to HTTP responses

- Add X-Content-Type-Options: nosniff
- Add X-Frame-Options: DENY
- Add X-XSS-Protection: 1; mode=block
- Add Referrer-Policy: strict-origin-when-cross-origin
- Add Strict-Transport-Security header when SSL is enabled

Security headers protect against common web vulnerabilities including
XSS attacks, clickjacking, and MIME-type sniffing.

All headers are automatically applied to all HTTP responses.
```

---

## Commit 3: Input Validation Library

**Type:** `security`  
**Priority:** High

**Files:**
- `bin/core/src/helpers/validation.rs` - New validation module
- `bin/core/src/helpers/mod.rs` - Add validation module
- `bin/core/src/auth/local.rs` - Apply validation to login/signup
- `bin/core/src/api/user.rs` - Apply validation to API key creation

**Message:**
```
security: Add comprehensive input validation library

- Create validation module with functions for username, password, URL, API key names, and variable names/values
- Add MongoDB sanitization functions to prevent NoSQL injection
- Apply validation to critical endpoints (user registration, login, API key creation)
- Add MongoDB field value validation with length and character checks

Validation functions include:
- validate_username() - Format and length validation
- validate_password() - Minimum length validation
- validate_url() - URL format validation
- sanitize_for_mongodb() - Defense-in-depth against NoSQL injection
- validate_mongodb_field_value() - Safe field value validation

This addresses the security recommendation for explicit input validation
on all user inputs.
```

---

## Commit 4: Rate Limiting

**Type:** `security`  
**Priority:** High

**Files:**
- `bin/core/src/helpers/security.rs` - Rate limiter implementation
- `bin/core/src/api/auth.rs` - Apply rate limiting to auth endpoints

**Message:**
```
security: Implement rate limiting for authentication endpoints

- Create RateLimiter struct with configurable limits (default: 5 attempts per 5 minutes)
- Add rate_limit_middleware for Axum integration
- Apply rate limiting to all /auth/* endpoints
- Automatically reset rate limit on successful authentication
- Add get_client_ip() helper to extract IP from headers (X-Forwarded-For, X-Real-IP)
- Log rate limit events as security events

Rate limiting protects against brute-force attacks on authentication endpoints.
Returns HTTP 429 (Too Many Requests) when limit is exceeded.

This addresses the security recommendation for rate limiting on API endpoints.
```

---

## Commit 5: Security Logging

**Type:** `security`  
**Priority:** Medium

**Files:**
- `bin/core/src/auth/local.rs` - Add security logging to login
- `bin/core/src/helpers/security.rs` - log_security_event function

**Message:**
```
security: Add comprehensive security event logging

- Add log_security_event() function for structured security logging
- Log failed login attempts with IP addresses
- Log database query errors during authentication
- Log rate limit exceeded events
- Log successful logins at INFO level

Security events are logged with format:
SECURITY EVENT | type: <event_type> | ip: <ip_address> | details: <details>

This provides an audit trail for security-related events and helps
identify potential security issues.
```

---

## Commit 6: Session Management (Clock Skew)

**Type:** `security`  
**Priority:** Low

**Files:**
- `bin/core/src/auth/mod.rs` - JWT clock skew tolerance
- `bin/core/src/auth/jwt.rs` - Exchange token clock skew tolerance

**Message:**
```
security: Add clock skew tolerance for JWT tokens and API keys

- Add 5-minute clock skew tolerance for JWT token expiration checks
- Add 1-minute clock skew tolerance for exchange token expiration
- Add 5-minute clock skew tolerance for API key expiration checks

Clock skew tolerance prevents token rejection due to minor time
differences between servers, improving user experience while maintaining
security. This is a standard practice for JWT implementations.

This addresses the security analysis recommendation for clock skew
tolerance in token validation.
```

---

## Commit 7: Error Handling Improvements

**Type:** `refactor`  
**Priority:** Medium

**Files:**
- `bin/core/src/config.rs` - Config parsing error handling
- `bin/core/src/startup.rs` - Startup error handling
- `bin/core/src/state.rs` - DB initialization error handling
- `bin/core/src/monitor/mod.rs` - Config validation
- `bin/core/src/monitor/resources.rs` - Lock error handling

**Message:**
```
refactor: Improve error handling in critical startup paths

- Replace unwrap() with proper error handling in config parsing (fallback to default)
- Improve error messages for database initialization failures
- Add graceful error handling for startup cleanup operations
- Replace unwrap() with error handling in monitor cache locks
- Add better error context using anyhow::Context

Improved error handling provides:
- Better diagnostics for configuration issues
- Graceful degradation for non-critical failures
- More actionable error messages
- Prevention of unexpected panics

This addresses the code analysis finding of 67 unwrap()/expect() calls
in critical paths.
```

---

## Commit 8: TypeScript Type Safety

**Type:** `refactor`  
**Priority:** Medium

**Files:**
- `frontend/tsconfig.json` - Enable noImplicitAny
- `frontend/src/ui/json.tsx` - JsonValue type
- `frontend/src/lib/hooks.ts` - Event handler types
- `frontend/src/monaco/init.ts` - Type assertion

**Message:**
```
refactor: Improve TypeScript type safety

- Enable noImplicitAny in tsconfig.json for stricter type checking
- Replace 'any' type with JsonValue type in Json component
- Replace 'any' type with HTMLElement | null in event handlers
- Improve type assertion for Monaco compiler options

Type improvements:
- Json component now uses JsonValue type instead of any
- Event handlers use proper DOM element types
- Monaco configuration uses correct type assertions

This addresses the code analysis finding of 102+ 'any' type usages
and improves type safety throughout the frontend.
```

---

## Commit 9: Action State Documentation

**Type:** `docs`  
**Priority:** Low

**Files:**
- `bin/core/src/helpers/action_state.rs` - Documentation

**Message:**
```
docs: Add comprehensive documentation for action state management

- Add module-level documentation explaining thread-safe state management
- Document ActionState struct with safety guarantees
- Document UpdateGuard RAII pattern and deadlock prevention
- Add examples and usage patterns

Documentation explains:
- RAII pattern for automatic lock release
- Lock poisoning handling
- Per-resource lock isolation (no deadlock risk)
- UpdateGuard behavior and safety guarantees

This improves code maintainability and helps developers understand
the concurrency patterns used in the codebase.
```

---

## Commit 10: Analysis Documentation

**Type:** `docs`  
**Priority:** Low

**Files:**
- `ANALYSE_BERICHT.md` - Code analysis report
- `RACE_CONDITION_ANALYSE.md` - Concurrency analysis
- `SECURITY_IMPROVEMENTS.md` - Security improvements overview

**Message:**
```
docs: Add security and code analysis documentation

- Add ANALYSE_BERICHT.md with comprehensive code analysis
- Add RACE_CONDITION_ANALYSE.md with concurrency analysis
- Add SECURITY_IMPROVEMENTS.md with detailed security improvements

Analysis documents include:
- Code quality assessment
- Security findings and recommendations
- Performance analysis
- Architecture review
- Race condition analysis
- Lock mechanism evaluation

These documents provide comprehensive overview of codebase quality
and security status.
```

---

## Commit 11: Developer Documentation

**Type:** `docs`  
**Priority:** Low

**Files:**
- `docs/` directory - All developer documentation
- `README.md` - Updated with security section
- `DOCUMENTATION_SUMMARY.md` - Documentation overview

**Message:**
```
docs: Add developer documentation and guides

- Add docs/DEVELOPER_GUIDE.md with comprehensive developer documentation
- Add docs/API_SECURITY.md with API security documentation
- Add docs/CHANGELOG_SECURITY.md with security changelog
- Add docs/README.md as documentation index
- Update main README.md with security section
- Add DOCUMENTATION_SUMMARY.md with documentation overview

Developer documentation includes:
- Security features overview
- Input validation API reference
- Rate limiting implementation details
- Error handling best practices
- API security guidelines
- Migration guides

This provides comprehensive documentation for developers, security
auditors, and contributors.
```

---

## Pull Request Organization

### PR 1: Critical Security Fixes (Commits 1-2)
- CORS Configuration
- Security Headers

### PR 2: Authentication Security (Commits 3-5)
- Input Validation
- Rate Limiting
- Security Logging

### PR 3: Session & Error Handling (Commits 6-7)
- Clock Skew Tolerance
- Error Handling Improvements

### PR 4: Code Quality (Commit 8)
- TypeScript Type Safety

### PR 5: Documentation (Commits 9-11)
- Code Documentation
- Analysis Reports
- Developer Guides

---

## Review Recommendations

1. **Review PR 1 first** - Critical security fixes
2. **Review PR 2 next** - Authentication security
3. **Review PR 3** - Session management and error handling
4. **Review PR 4** - TypeScript improvements
5. **Review PR 5** - Documentation (can be reviewed in parallel)

